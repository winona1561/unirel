# 实体类型元数据与 Crosswalk：数据负责人请求与核验说明

日期：2026-09-22

适用项目：UniRel CTI 关系抽取研究（CTI-RSV Stage A-repair）  
面向对象：数据负责人、数据保管方、上游数据构建人员、许可证或合规负责人  
当前状态：仅请求资产盘点与可用性确认；不代表授权导入、标注、训练或访问受保护数据。

## 1. 请数据负责人完成的事项

请确认是否持有能够与本项目获准训练视图对接的以下资产：

1. 实例级实体类型表：能将具体上下文中的具体实体实例映射到一个或多个实体类型，而不是只有类型词典或汇总计数。
2. 实体 crosswalk：能把上述实体记录稳定对应到原始来源记录，以及本项目的“实体—上下文单位”或具体 mention。
3. 许可证与来源说明：能说明数据从哪里取得、允许如何使用、由谁或什么流程产生，以及当前版本如何追溯。

现阶段不要求立即发送数据文件。请先使用第 11 节模板回复“完整存在、部分存在、不存在或需要进一步盘点”。若资产可能存在，再按第 9 节准备清单并通过获批渠道交付。

本文件把“盘点回复”和“数据交付”严格分开：

- A“完整存在”按数据负责人本次声明的资产范围判断：该范围内全部相关记录均有实例类型和 crosswalk，并具备许可证与来源说明。未来白名单只决定其中哪些记录可以交付，不改变本次盘点对资产完整性的判断。
- B“部分存在”包括只覆盖部分记录、缺少部分字段、只有类型表没有 crosswalk、许可证待确认，或部分记录的类型来源不独立。
- 当前不在本文中公开具体记录白名单。资产可能存在时，项目联系人将先与数据负责人核对资产范围，再签发精确到文件和记录范围的交付白名单。
- 未取得该白名单和批准渠道前，只回复资产级信息，不发送正文、逐记录 ID、逐记录哈希或数据文件。

## 2. 请求背景

本项目正在开展 CTI 关系语义研究的 Stage A-repair。旧 Stage A 准入仍未通过，stage_b_allowed=false，当前不得启动模型训练。

现有获准本地资产中：

- fold02/fold03 两个训练视图共包含 26,480 次候选实体端出现；
- 当前存储的 type 和 type_candidates 均可由实体表面字符串正则完整重算；
- 没有独立的 type_source、STIX 对象 ID、人工裁决来源或实体 crosswalk；
- 两折合并有 1,387 个去重“实体—上下文单位”，其中 929 个为 Unknown；这是人工试点规模统计，不是 Unknown 准入分母；
- Unknown 准入分母是每个候选行的主、客实体端出现次数：fold02 为 7,930/13,284，fold03 为 7,804/13,196；修复后须按各折相同实体端分母分别不高于 25%，并同时具备独立质量证据。

本请求不是收集更多关系标签，而是确认是否已存在可以独立验证实体类型的上游资产。若存在并通过核验，它可能替代或缩小后续最多 200 个单位的人工类型试点；若不存在，项目才会另行申请 P2 双人盲核验授权。

## 3. 关键术语

### 3.1 实例级实体类型表

“实例级”表示类型结论必须落到可定位的实体实例上，例如“来源报告 R 的第 N 个 mention”或“文本组 G 中规范化实体 E”。以下内容不属于实例级类型表：

- 类型名称列表、本体定义或聚合数量；
- 不带来源记录和实体定位信息的字符串词典；
- 无法区分同名实体在不同上下文中含义的全局映射。

### 3.2 实体—上下文单位

本项目当前按“文本组或报告标识 + 规范化实体”形成去重单位。同一表面字符串出现在不同上下文时，不自动视为同一单位。若上游采用不同单位，请提供足够字段，使我们能建立明确的一对一、一对多或多对一映射。

### 3.3 Entity crosswalk

Crosswalk 是一张可审计的映射表，用于说明上游实体来自哪个原始记录、对应本项目哪个实体—上下文单位或 mention、使用什么匹配键，以及歧义如何保留和处理。仅凭模糊字符串相似度自动匹配且没有匹配证据或歧义状态的结果，不视为合格 crosswalk。

### 3.4 独立实体类型来源

类型不得由本项目的关系 gold、relation_silver、隐藏正例清单、模型关系预测或关系 domain/range 约束反推。例如，不能因为某实体是 targets 的客体，就自动标为 Target。

可以考虑的独立来源包括：

- 原始数据发布方提供的实体类型或 STIX 对象类型；
- 带稳定对象 ID 和来源记录的上游实体表；
- 有明确指南、标注者和裁决记录的人工实体类型标注；
- 其他不依赖本项目关系标签、且能说明生成过程和质量的实例元数据。

模型预测类型本身不是独立金标。本轮未授权使用 GLiNER、LLM 或其他模型伪类型替代人工或可追溯元数据。

## 4. 当前目标类型词表

当前项目使用以下 16 个实体类型 ID：

| 类型 ID | 含义摘要 |
|---|---|
| ThreatActor | 已命名的攻击组织、入侵集、APT 或威胁行为者 |
| Campaign | 已命名的攻击活动或行动 |
| Malware | 恶意软件家族、勒索软件、加载器、后门或植入体 |
| Tool | 攻击工具、双用途工具或 living-off-the-land 工具 |
| Technique | ATT&CK 技术编号或技术行为短语 |
| Vulnerability | CVE 或其他漏洞标识 |
| CourseOfAction | 缓解、补丁、修复、防御措施或安全建议 |
| Report | 威胁报告、公告、分析产品或情报产品 |
| Indicator | IP、域名、URL、邮箱、哈希等 IOC 值 |
| Infrastructure | C2、服务器、代理、托管或其他攻击基础设施 |
| Target | 受害目标、平台、产品或资产类型 |
| Industry | 被攻击或涉及的行业部门 |
| Organization | 公司、政府部门、厂商、机构等组织 |
| Location | 国家、地区、城市或地缘位置 |
| Time | 日期、月份、年份或其他时间表达式 |
| Unknown | 依据现有信息不能可靠归入上述类型 |

如上游类型体系不同，请不要强行改写成这 16 类。请同时提供原始类型、类型体系版本和映射建议；映射是否采纳由后续独立核验决定。允许保留 Unknown、Ambiguous 或多个候选类型，不要求为提高覆盖率强制单选。

## 5. 实例级类型表的最低字段要求

字段名可以不同，但语义必须能够对应。推荐使用 UTF-8 CSV、TSV、JSONL 或 Parquet。

| 字段 | 必需性 | 说明 |
|---|---|---|
| source_dataset_id | 必需 | 数据集或资产的稳定名称与版本 |
| source_record_id | 必需 | 原始报告、文本记录或对象的稳定 ID |
| source_entity_id | 强烈建议 | 上游实体或 STIX 对象的稳定 ID；没有时须说明 |
| entity_text | 必需 | 原始实体表面文本 |
| normalized_entity | 建议 | 上游规范化值及规范化规则版本 |
| mention_start / mention_end | 条件必需 | 若按 mention 对齐，说明偏移区间和编码口径 |
| context_locator | 必需 | 句、段、文本组 ID 或安全的上下文引用 |
| source_type | 必需 | 上游原始类型，不只保留项目映射类型 |
| source_type_system | 必需 | 类型体系名称与版本 |
| project_type_candidate | 可选 | 到本项目 16 类的建议映射；不是自动真值 |
| type_status | 建议 | accepted、unknown、ambiguous、conflicting 等 |
| type_source | 必需 | 发布方字段、人工标注、对象类型或其他来源 |
| annotator_or_process | 条件必需 | 人工或自动流程说明；可用匿名标识 |
| confidence | 可选 | 仅在有定义和校准说明时提供 |
| created_at / updated_at | 建议 | 生成和最后更新时间 |
| provenance_ref | 必需 | 许可证、原始对象、标注批次或审计记录引用 |

不得把关系标签、关系方向或关系 schema 推断结果填入 type_source，也不得用缺失来源的高置信度分数代替可追溯来源。

偏移字段必须随表声明编码单位和区间约定。若由本项目生成，默认采用规范化前原文的 Unicode code point、零基、左闭右开区间 [start, end)；若上游采用 UTF-16、字节偏移、token 偏移或闭区间，请保留上游原值并明确说明，不能静默换算。

### 示例记录（仅示意格式，不是项目数据）

    {
      "source_dataset_id": "example-cti-corpus@2026-01",
      "source_record_id": "report-000123",
      "source_entity_id": "malware--example-stable-id",
      "entity_text": "ExampleLoader",
      "normalized_entity": "exampleloader",
      "mention_start": 418,
      "mention_end": 431,
      "context_locator": "paragraph-7",
      "source_type": "malware",
      "source_type_system": "publisher-schema-2.0",
      "project_type_candidate": "Malware",
      "type_status": "accepted",
      "type_source": "publisher_entity_annotation",
      "annotator_or_process": "publisher-release-pipeline",
      "provenance_ref": "annotation-batch-2026-01"
    }

## 6. Crosswalk 的最低字段要求

Crosswalk 可单独成表，也可与类型表合并。至少应包含：

| 字段 | 必需性 | 说明 |
|---|---|---|
| source_dataset_id | 必需 | 与类型表一致的数据集和版本 |
| source_record_id | 必需 | 上游记录 ID |
| source_entity_id | 建议 | 上游稳定实体 ID |
| source_mention_id | 条件必需 | 若上游区分 mention，提供稳定 mention ID |
| project_text_group_id | 必需或可生成 | 本项目文本组/报告单位；生成规则须可复现 |
| project_entity_key | 必需或可生成 | 本项目实体—上下文单位；生成规则须可复现 |
| project_mention_locator | 建议 | 字符偏移、句 ID 或其他 mention 定位信息 |
| match_method | 必需 | 稳定 ID、精确偏移、规范化键或人工确认等 |
| match_status | 必需 | exact、one_to_many、many_to_one、ambiguous、unmatched |
| match_evidence | 必需 | 建立映射的证据或规则版本 |

Crosswalk 必须保留未匹配和歧义记录，不能只导出成功匹配部分后把覆盖率写成 100%。如使用字符串或偏移匹配，请说明 Unicode、大小写、空白、重复 mention、重叠 mention 和偏移区间的处理规则。

盘点阶段不要求数据负责人自行生成 project_text_group_id 或 project_entity_key。若上游没有这些字段，请提供稳定的来源记录/实体/mention ID 和可复现定位信息；项目方只会在精确白名单和数据访问授权后生成项目侧键，并保存生成规则版本。数据负责人不得为获得项目键而读取其无权访问的项目文件。

## 7. 许可证、用途与数据血缘要求

请为每项资产提供：

1. 数据拥有方、发布方和实际提供方；
2. 数据集名称、版本、发布日期和交付日期；
3. 许可证全文、链接或书面授权；
4. 是否允许学位研究、内部分析、模型训练和结果发表；
5. 是否允许生成并保存派生 crosswalk、聚合统计和校验哈希；
6. 不可再分发、不可导出、限定机器或到期删除等限制；
7. 类型生成流程及人工复核情况；
8. 已知错误、遗漏、版本迁移或撤销记录；
9. 文件清单、文件大小、行数和 SHA256；
10. 数据负责人或合规联系人的确认日期。

“公开可下载”不自动等于可用于本项目。缺少明确许可证或授权范围时，资产只能登记为待核验，不能导入。

## 8. 不足以替代 P2 的材料

- 由关系 gold、silver、关系预测或关系 domain/range 反推的类型；
- GLiNER、LLM、NER 模型或规则系统生成但未经独立验证的伪类型；
- 当前项目已使用的表面字符串正则重算结果；
- 只有实体字符串和类型、没有上下文或来源记录的词典；
- 只有类型 schema、本体、关系约束或聚合数量；
- 无许可证、来源不明或版本不明的数据副本；
- 把 Unknown 统一改成泛化大类；
- 删除歧义、未匹配或困难实例后的选择性结果；
- 需要先打开 outer、Task105、锁定评测、未授权 heldout 或历史 508 条 OOF 才能构造的资产。

若资产只有部分记录受关系标签或模型输出污染，可由数据负责人在源端隔离未污染部分，并同时提交排除规则和数量；不得将干净与污染记录混在同一交付文件中。模型预测经独立人工复核后，只能作为“人工确认、模型辅助”的记录提交，必须保留模型来源、人工判断和分歧信息；它不会自动成为独立金标，是否可用仍需另行核验。

## 9. 建议交付包

若确认资产存在，请先提交不含受保护正文的资产清单，再协商获批传输范围。完整交付包建议包含：

1. README：内容、版本、联系人和使用限制；
2. 实例级实体类型表；
3. 实体 crosswalk；
4. 数据字典和类型体系说明；
5. 类型体系到本项目 16 类的映射建议及未映射项；
6. 许可证或书面授权；
7. 生成、标注、复核和裁决流程；
8. 覆盖率、Unknown、歧义和冲突统计；
9. 文件清单、行数、大小和 SHA256；
10. 已知限制和不应作出的解释。

初次盘点阶段请不要发送 outer、Task105、锁定评测、未授权 heldout 或包含它们内容的混合归档。若资产混有允许范围与保护范围，应由获授权的数据保管方在源端生成只含获准记录的新导出；研究执行人员不得先读取完整文件再过滤。

初次资产清单只能提供第 11 节模板所列的资产级元数据与汇总信息，包括资产名称和版本、拥有方/发布方、保管环境类别和获准联系人、聚合覆盖范围、字段和格式、类型体系与生成流程、污染和人工复核状态、许可证及用途状态、保护范围、资产级文件统计与哈希、确认人和日期。不要提供受保护记录的逐条 ID、正文片段或可用于反查内容的逐记录哈希。许可证尚不可判定时，请保留原文件在现有受控位置，仅回复“待核实”，不要传输副本。后续如判定不可使用，由数据负责人按原有保管政策保留或处置，项目方不接收、不留存待定副本。

初次回复中的保管信息只写环境类别（如“机构受控服务器”）以及负责人角色或获准联系人；不得填写绝对路径、访问凭据、内部网络地址或未经允许的个人信息。“可覆盖范围”只写数据集版本、时间范围和聚合数量，不列逐条记录 ID。

## 10. 收到资产后的核验方式

提供资产不等于自动准入，也不等于授权训练。项目将在另行登记精确白名单后依次进行：

1. 许可证核验；
2. 文件清单、行数、schema、版本和 SHA256 核验；
3. 逐条来源与标注批次可追溯性核验；
4. 不依赖关系 gold/silver、隐藏正例或关系预测的独立性核验；
5. Crosswalk 的 exact、歧义、未匹配、一对多和多对一统计；
6. 在原实体端分母上重算 Unknown；
7. 用与设计过程隔离的确认样本估计质量：已接受类型加权精度目标至少 95%，按文本组重采样的 95% 区间下界至少 90%；
8. 重算原六个预注册语义组的有序类型交集和支持度；
9. 记录资产是替代 P2、缩小 P2，还是仍不足以使用。

覆盖和质量必须同时满足。即使 Unknown 能降到 25% 以下，来源或质量不合格也不能宣布通过；少量高精度记录若覆盖不足，同样不能替代 P2。

Unknown 复算按每次候选实体端出现计数，每个端在原分母中只计一次，不因 crosswalk 的一对多或多对一映射重复计数。只有当该端能够通过获准 crosswalk 获得至少一个独立、许可证合格且已接受的项目类型，并能按冻结规则确定 primary type 时，才计为非 Unknown。unmatched、ambiguous、conflicting、多候选未裁决、来源无许可或映射证据不足的端一律计为 Unknown。跨折或重复实体—上下文单位可以共享同一来源裁决，但须投影回各折原有端出现分别计数，不改变任何折的分母。

原语义组门槛保持不变：六个组分别为 delivery_actions、possession_control_use、network_transfer、attribution_authorship、location_origin 和 use_exploit；至少两个组需要在 fold02 与 fold03 同时达标。每个达标组至少包含两个关系；每个关系在每折至少有 10 个可见正例、覆盖至少 5 个独立文本指纹，并存在能够支持组内比较的有序类型交集。提供类型资产不会增加关系正例或独立文本，因此不能单独保证语义组通过。

## 11. 数据负责人回复模板

请复制以下模板回复。尚不确定的项目填写“待核实”，不要根据猜测填写“是”。

请通过收到本文的原沟通渠道返回文字模板，不附数据文件。项目确认资产可能可用后，将另行指定联系人、精确白名单、批准交付渠道和传输期限。

    【资产可用性】
    [ ] A. 在本次声明资产范围内，完整存在实例级实体类型表、crosswalk、许可证和来源说明
    [ ] B. 只存在部分资产或只能覆盖部分数据
    [ ] C. 不存在符合要求的资产
    [ ] D. 需要进一步盘点，预计回复日期：____年__月__日

    【资产基本信息】
    数据集/资产名称：
    版本与日期：
    数据拥有方/发布方：
    保管环境类别与负责人角色/获准联系人（不得填写路径或凭据）：
    可覆盖的记录范围（仅聚合描述，不列逐条 ID）：
    预计实体记录数：
    是否有稳定 source_record_id：是 / 否 / 待核实
    是否有稳定 source_entity_id 或 STIX ID：是 / 否 / 待核实
    是否有 mention 偏移或上下文定位：是 / 否 / 待核实
    是否已有与本项目记录的 crosswalk：是 / 否 / 部分 / 待核实

    【类型来源与独立性】
    类型由何种流程产生：
    是否使用过本项目关系标签、relation_silver、隐藏正例或关系预测：是 / 否 / 待核实
    如使用过，请仅按资产级字段和聚合范围说明，不列逐条记录 ID：
    是否包含模型预测类型：是 / 否 / 待核实
    是否有人工作业指南、复核或裁决记录：是 / 否 / 待核实

    【许可证与用途】
    许可证或授权文件/链接：
    是否允许学位研究：是 / 否 / 待核实
    是否允许内部分析：是 / 否 / 待核实
    是否允许模型训练：是 / 否 / 待核实
    是否允许发表聚合结果：是 / 否 / 待核实
    是否允许保存派生 crosswalk 和哈希：是 / 否 / 待核实
    不可再分发、环境、期限或删除限制：

    【文件与格式】
    可提供的文件及格式：
    主要字段：
    类型体系名称与版本：
    Unknown/歧义如何表示：
    是否可提供文件行数、大小和 SHA256：是 / 否

    【保护范围确认】
    拟交付资产是否混有 outer、Task105、锁定评测、未授权 heldout 或历史 508 条 OOF：
    [ ] 否
    [ ] 是，但可以由数据保管方在源端生成只含获准范围的新导出
    [ ] 待核实

    【结论与下一步】
    建议：提供资产核验 / 先提供清单 / 确认不存在 / 需要补充讨论
    预计可提供清单的日期：
    其他限制或说明：
    确认人及日期：

## 12. 当前研究边界

本请求只用于资产盘点和后续准入判断，不改变以下状态：

- 旧 Stage A 仍为 0/6 语义组通过；
- stage_b_allowed=false；
- P2 人工试点尚未授权或启动；
- 不得用模型伪类型替代人工或可追溯元数据；
- 不得启动 P3、P4、P5、Stage B/C、六次 OOF 或其他训练；
- 不得读取 outer、Task105、锁定评测、未授权 heldout 或历史 508 条 OOF；
- 不得先读取混合保护文件再过滤；
- 不得修改现有关系标签、类型或旧失败审计。

数据负责人确认“没有此类资产”不会被视为数据工作失误；它只用于选择下一条合规路线。确认不存在后，项目将另行请求 P2 独立授权，并在获批前不生成正式标注样本、不分配标注任务、不写回类型。

## 13. 联系与交付规则

- 初次盘点回复：通过收到本文的原沟通渠道返回第 11 节文字模板，不附数据。
- 数据负责人：在回复中填写确认人、资产保管负责人以及许可证/合规联系人。
- 后续交付：仅在项目方另行发出精确白名单、指定联系人和批准交付渠道后进行。
- 本文没有设定默认回复截止日期；如转发方需要期限，应在转发消息中单独注明。
- 未经另行书面确认，任何共享盘、邮件附件、即时通信附件或仓库目录都不视为批准的数据交付渠道。
