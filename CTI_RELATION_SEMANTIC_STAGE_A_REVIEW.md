# CTI-RSV Stage A：定义、数据与信息源准入审计

**用途：** 供导师确认“面向不完整标注 CTI 的角色—行为语义关系验证”是否具备继续实现的条件。  
**日期：** 2026-09-21  
**审计范围：** development-only；只读取 inner fold 02/03 的训练视图及既有运行元数据。  
**安全边界：** 未读取 outer、Task105、锁定评测或测试数据；未训练、未调用模型、未改标签；输出不含原文、实体文本、report ID 或 pair ID。  
**当前决定：** Stage A 已完成但准入失败，Stage B CPU 合同和 Stage C 训练均不得启动。

## 1. 导师优先阅读顺序

1. **第 2 节：需要确认的五项决定。**
2. **第 3 节：准入门槛结果。**
3. **第 4 节：18 类定义对齐表。**
4. **第 5–7 节：支持度、输入合法性、OOF 与证据来源。**
5. **第 8 节：修复后建议实现的方法。**

本文件给出的是训练前可行性边界，不是新模型结果，也不能作为论文中的鲁棒性或准确率结论。

## 2. 需要导师确认的事项

### 2.1 关系定义与现有 schema 的三处冲突

请确认以下处理方式：

- authored_by：项目定义的 subject 是代码、工具、恶意软件或工件；现有 schema 的 domain 是 Report、Indicator、CourseOfAction，且遗漏 Malware/Tool。
- downloads：项目定义要求 object 是被下载对象，并明确不是 downloads_from；现有 schema 的 range 含 Indicator/Infrastructure，可能把来源端点混入 object。
- has：项目定义是严格的包含/能力关系；现有 schema 的 domain/range 都是通配符，不能约束角色。

建议：先修订**定义与 schema 映射**，不修改任何实例标签；由导师确认后再冻结语义卡。未经确认不实现 A3。

导师意见：____________________________________________________

### 2.2 实体类型来源

现有类型由实体表面字符串的正则规则推断，未使用关系 gold，因此没有关系标签泄漏；但 fold02/fold03 的 Unknown 实体率分别为 **59.70% / 59.14%**，超过预设上限 25%。

建议：批准一个独立、可追溯的实体类型来源或有限人工类型核验。不得依据关系标签反推类型，也不得降低门槛掩盖缺失。若无法获得可靠类型，A1“类型字段控制”不能作为有效对照，主张需收缩。

导师意见：____________________________________________________

### 2.3 语义组支持度

当前 6 个预注册多关系语义组中 **0 个**同时通过两折的关系支持度和非 Unknown 有序类型重叠门槛。建议保持门槛，先解决类型来源，再重算；若仍不足，只能通过另行批准、许可与血缘明确的 development 训练数据增加支持，或收缩研究问题。

导师意见：____________________________________________________

### 2.4 长文本输入策略

在固定上限 384 tokens 下，fold02/fold03 分别有 **64.02% / 64.23%** 的 pair_text 超长；P50 已为 938 tokens。虽然实体位置在完整 passage 中逐字对齐率为 100%，直接头部截断仍可能丢失关系行为证据。

建议：先设计并合成验证“实体中心窗口 + 两端实体保留 + 截断状态显式记录”的输入合同，再谈编码器或 GPU 训练。

导师意见：____________________________________________________

### 2.5 development-only OOF

现有 D00 fold02/03 输出加载的是在 inner split 之前训练的全训练 checkpoint，属于 train-fit，不能冒充 OOF。历史 5 折 OOF 覆盖全部 508 条 pre-v2 文本记录，其中包含后来保护的 outer；本轮遵守禁止访问 outer 的约束，没有读取该预测文件。

建议：若要完成 Stage A 的训练侧错误分析，另行批准只在 development 白名单内、按 report 分组、每折重新训练的 OOF 生成。批准前保持该项缺失。

导师意见：____________________________________________________

## 3. Stage A 门槛结论

| 门槛 | 结果 | 结论 |
|---|---:|---|
| 18 类定义覆盖 | 18/18 | 覆盖完整 |
| 定义与 schema 一致 | 15/18 | 失败：3 类冲突 |
| 至少 2 个语义组通过 | 0/6 | 失败 |
| 类型 Unknown 率不高于 25% | 59.70% / 59.14% | 失败 |
| 两端实体位置可靠率至少 99% | 100% / 100% | 通过 |
| 384-token 输入无需未定义截断 | 超长 64.02% / 64.23% | 失败 |
| 可用 development-only OOF | 0 | 失败 |
| outer/锁定数据访问 | 0 | 安全边界通过 |

**决定：** stage_b_allowed=false。禁止开始 Stage B CPU 合同、Stage C 20 单元筛选或任何 GPU 训练。

## 4. 18 类定义对齐表

定义来源为项目现有标注规范，不新增实例标签；专家确认仍待完成。

| 关系 | subject 角色 → object 角色 | 行为边界 | schema 状态 |
|---|---|---|---|
| attributed_to | 被归因实体/活动 → 责任主体或来源 | 相似或共现不足 | 一致 |
| authored_by | 代码/工具/恶意软件/工件 → 作者或开发者 | 使用不等于编写 | **冲突** |
| communicates_with | 通信软件/主体 → 通信端点 | 提及或解析不足 | 一致 |
| controls | 控制者/运营者 → 被控资产 | 区分 owns/uses | 一致 |
| delivers | 投递机制/载体 → 载荷或工件 | 区分 downloads/drops | 一致 |
| downloads | 下载者 → 被获取对象 | 不能只编码来源端点 | **冲突** |
| drops | 写入者/释放者 → 落地工件 | 传输本身不足 | 一致 |
| exfiltrates_to | 外传主体/软件 → 目的地/接收方 | 普通 C2 不足 | 一致 |
| exploits | 利用者 → 漏洞/弱点/机制 | 漏洞存在本身不足 | 一致 |
| has | 拥有者/容器 → 组件/能力/属性 | 不能作兜底标签 | **冲突** |
| impersonates | 冒充者 → 被冒充身份/组织 | 名称相似不足 | 一致 |
| indicates | 威胁指标 → 被指向威胁实体 | 普通“表明”不足 | 一致 |
| located_at | 被定位实体 → 当前位置 | 区分来源 | 一致 |
| originates_from | 有来源的实体 → 来源位置 | 语言/时区推断不足 | 一致 |
| owns | 所有者 → 被拥有资产 | 运营、控制或使用不足 | 一致 |
| targets | 攻击者/活动/恶意软件 → 目标/受害者 | 讨论或偶然影响不足 | 一致 |
| uses | 主体/活动/软件 → 工具/技术/资源 | 优先采用更具体动作 | 一致 |
| variant_of | 变体 → 父家族/早期形式 | 功能或代码相似不足 | 一致 |

## 5. 每类训练支持度

预设门槛为：每个 fold 至少 10 个可见正例单元且至少 5 个不同训练文本指纹。

| 关系 | fold02 正例/指纹 | fold03 正例/指纹 | 两折支持度 |
|---|---:|---:|---|
| attributed_to | 19 / 12 | 19 / 14 | 通过 |
| authored_by | 12 / 11 | 13 / 9 | 通过 |
| communicates_with | 12 / 12 | 12 / 12 | 通过 |
| controls | 1 / 1 | 0 / 0 | 失败 |
| delivers | 1 / 1 | 4 / 2 | 失败 |
| downloads | 7 / 2 | 3 / 1 | 失败 |
| drops | 10 / 7 | 11 / 6 | 通过 |
| exfiltrates_to | 3 / 3 | 3 / 3 | 失败 |
| exploits | 1 / 1 | 0 / 0 | 失败 |
| has | 1 / 1 | 0 / 0 | 失败 |
| impersonates | 4 / 1 | 5 / 2 | 失败 |
| indicates | 86 / 18 | 80 / 18 | 通过 |
| located_at | 9 / 5 | 10 / 5 | 失败 |
| originates_from | 6 / 6 | 7 / 7 | 失败 |
| owns | 2 / 2 | 3 / 2 | 失败 |
| targets | 221 / 128 | 222 / 127 | 通过 |
| uses | 376 / 216 | 379 / 212 | 通过 |
| variant_of | 18 / 16 | 19 / 17 | 通过 |

支持度通过不代表语义组准入。attributed_to/authored_by 两类数量够，但在当前非 Unknown 类型中没有共享的有序类型对；其余多关系组至少含一个低支持关系。

## 6. 输入、位置和截断审计

| 项目 | fold02 | fold03 |
|---|---:|---:|
| 训练候选行 | 6,642 | 6,598 |
| 正例候选行 | 786 | 787 |
| Unknown 实体率 | 59.70% | 59.14% |
| 两端实体全部逐字定位率 | 100% | 100% |
| token P50 / P95 / 最大 | 938 / 1681 / 2098 | 938 / 1680 / 2098 |
| 超过 384 tokens | 4,252（64.02%） | 4,238（64.23%） |
| 有 candidate evidence 的行 | 6,642 | 6,598 |
| 正例中有 evidence sentence ID | 786 | 787 |
| 正例中有同时含两实体的候选句 | 561 | 544 |

实体坐标是 pair 构造时对完整文本或证据窗做不区分大小写的字面搜索所得。位置可用，但目前不等于模型在 384-token 截断后仍能看到对应行为证据。

## 7. 信息源和 OOF 边界

- subject/object.type：实体表面字符串正则推断；独立于关系 gold，但不是外部或专家核验类型。
- evidence_sentence_ids：subject/object mention 所在句 ID 的并集，不是人工关系证据 gold。
- candidate_evidence：实体共现、触发词、位置等启发式；train/dev 的 relation_silver 会使用现有 gold，不能作为独立证据监督。
- 时间监督：无。
- 否定监督：无。
- 人工 evidence gold：无。
- 可准入 OOF：无；D00 是 train-fit，历史全量 OOF 因包含当前 outer 而未读取。

因此，当前可以安全使用的是实体位置与启发式候选信息；不能报告 evidence F1、时间/否定理解能力，也不能把 silver evidence 当独立人工监督。

## 8. 修复后建议进入的方法

主候选仍是 **CTI-RSV（CTI Relation Semantic Verification）**，但必须在五个阻塞项闭合后才实现：

1. **实体中心输入构造。** 以两端实体的已验证位置为锚点，构造不超过 384 tokens 的上下文；确保两实体、局部行为证据和方向标记都被保留，并对无法同时保留的样本 fail closed。
2. **经确认的角色—行为卡。** 只使用导师批准的定义映射；冲突关系在修订前不生成卡片。
3. **分离编码。** 文本实体对与关系语义卡分开编码，由共享 verifier 评分；禁止旧 parent logit 旁路直接决定输出。
4. **匹配控制。** A0 为同容量 relation-ID 验证器，A1 为可靠类型字段加无意义行为占位，A2 为一般描述，A3 为真实 CTI 角色—行为卡，A4 为固定语义错配。
5. **PU 延后。** Stage C 先统一 B0 目标验证语义结构；标准 PU 与 DSC 只在 Stage D 另行比较。DSC 不是当前主方法，也不能用于绕过 Stage A。

如果可靠类型、两组语义支持或 development-only OOF 无法获得，应收缩为“输入与语义适配边界”研究，不启动 20 单元训练矩阵。

## 9. 下一次可授权的工作

下一项应是一个**不训练的 Stage A 修复协议**，包含：

- 3 个 schema 冲突的导师决定；
- 实体类型独立来源与许可；
- 实体中心窗口的 CPU 合成合同；
- development-only OOF 的单独生成提案与精确预算。

只有修复后重跑 Stage A 并通过，才进入 Stage B。当前文档不构成任何训练授权。

## 10. 权威产物

- 冻结：cti_improvement/research_protocol/cti_relation_semantic_stage_a_freeze.json
- 审计器：tools/audit_cti_relation_semantic_feasibility.py
- 合成测试：tests/test_cti_relation_semantic_feasibility.py
- 聚合结果：cti_improvement/research_protocol/cti_relation_semantic_stage_a_audit.json

导师总体意见：

____________________________________________________________

____________________________________________________________

日期：________。本次批准的下一步：________________________________