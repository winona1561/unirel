# CTI 关系抽取观测状态训练：阶段结果、待确认事项与新方法建议

**用途：** 供导师快速评审当前负结果、失败机制证据，以及是否进入新的算法分支。  
**日期：** 2026-09-21  
**实验范围：** development-only；未访问 outer、Task105 或任何锁定评测。  
**结论状态：** 当前 T0 方法失败并停止扩展；该负结果与失败审计不足以单独构成论文贡献，需要新方法与新的冻结实验协议。

---

## 1. 建议导师优先阅读的部分

若时间有限，请依次阅读：

1. **第 2 节：需要导师确认的研究决策**——决定下一阶段是否继续以及按什么规模继续。
2. **第 4 节：三臂核心结果**——确认当前失败是否真实，以及对照是否有效。
3. **第 5 节：严格失败审计**——确认新方法是否针对已有证据，而不是事后猜测。
4. **第 7 节：推荐的新方法**——确认方法方向、贡献表述和最小实验路线。
5. **第 9 节：建议冻结的下一阶段实验计划**——确认算力投入与停止条件。

第 11 节仅用于复现与核验，不需要首次讨论时逐项阅读。

---

## 2. 需要导师确认的研究决策

请导师重点确认以下六项。每项均会改变后续实现或实验成本。

### 2.1 是否继续以“不完整正标注下的 CTI 多标签关系学习”为论文主问题

- [ ] **同意继续。** 保留观测正例 / 未解析状态建模，开发新的训练目标。
- [ ] **不同意继续。** 停止该算法方向，将已有结果仅作为内部失败记录。

建议：继续，但必须用新方法替代当前 T0，不能把 T0 负结果本身包装成贡献。

### 2.2 是否认可当前 B0/B1 为“同模型、同输入、同预算”的匹配对照

- B0：把所有未解析关系当作负类，使用 relation BCE。
- B1：row-level PU relation risk。
- T0：当前失败的 classwise nnPU relation risk。

这些对照用于隔离 relation-head objective 的影响，不等同于复现历史完整 B0/B1 系统。

- [ ] 认可这种匹配对照设计。
- [ ] 需要同时加入历史完整系统对照；请明确需要复现的系统和指标。

### 2.3 是否同意把“双流覆盖感知 classwise nnPU”作为下一主方法

- [ ] 同意，按第 7 节实现并命名为新的方法分支。
- [ ] 不同意；希望优先尝试：____________________。

### 2.4 是否同意固定主指标与禁止事后调阈值

建议主指标：固定阈值 0.5 的 inner-development micro-F1。  
辅助指标：precision、recall、遮蔽训练正例恢复率、每类有效更新覆盖率、零信号 batch 比例。

- [ ] 同意固定阈值，不用当前 development 结果调阈值。
- [ ] 希望增加预先冻结的阈值校准流程；需另设 calibration split，不能使用锁定评测。

### 2.5 是否接受建议的分阶段计算预算

- [ ] 接受第 9 节的“单单元机制门槛 → 小矩阵 → 多 seed 确认”。
- [ ] 需要缩减预算；可接受的 GPU 时间或最大训练单元数：____________________。

### 2.6 论文贡献的最低验收标准

建议至少满足：新方法在预先冻结的多个遮蔽率和多个 seed 上，相对 B0/B1 改善遮蔽正例恢复，并且 development micro-F1 不退化；报告均值、离散度、置信区间和配对比较。

- [ ] 同意该标准。
- [ ] 修改为：________________________________________________________。

---

## 3. 当前研究问题与实验设计

### 3.1 研究问题

训练数据只暴露“已观测正例”和“未解析关系”两种状态。未解析关系可能是真负例，也可能是缺失正例。研究目标是避免把全部未解析关系直接当负类，同时提高对隐藏正关系的恢复能力。

### 3.2 已冻结的共同条件

- 模型：`CTIMultilabelATGLModel`，只比较 relation head objective。
- 基础模型：本地 `bert-base-cased`。
- 数据单元：inner fold02、遮蔽率 0.1、seed 13。
- 训练预算：8 epochs，batch size 8，AdamW，学习率 `2e-5`。
- 解码阈值：固定 0.5；未在 heldout 上调参。
- 三个 arm 使用相同可见输入、初始化来源与训练预算。
- 所有训练和审计均为 development-only。

### 3.3 当前 T0 的关键计算方式

T0 对每个关系类别计算 classwise nnPU 风险：

`π_c R_c^+ + max(0, R_c^u - π_c R_c^-)`

当前实现有两个直接影响优化的条件：

1. 某类别在当前 minibatch 没有可见正例时，该类别在此步完全跳过。
2. 整个 minibatch 没有任何可见正例时，返回与计算图连接的零损失。

---

## 4. 三臂核心结果

以下结果来自完全相同的 `fold02 / mask=0.1 / seed=13 / cuda` 单元：

| Arm | Development Precision | Recall | Micro-F1 | TP / FP / FN | 遮蔽正例恢复 |
|---|---:|---:|---:|---:|---:|
| B0 | 0.7368 | 0.6000 | **0.6614** | 126 / 45 / 84 | 17 / 78 = 0.2179 |
| B1 | 0.7151 | 0.6095 | **0.6581** | 128 / 51 / 82 | 21 / 78 = 0.2692 |
| T0 | 0.0000 | 0.0000 | **0.0000** | 0 / 0 / 210 | 0 / 78 = 0.0000 |

### 4.1 可以成立的结论

1. T0 在该单元上发生实质性退化：固定阈值下没有任何正预测，也没有恢复任何遮蔽正例。
2. B0/B1 在相同模型、输入和阈值下均能输出有效正预测，因此失败不能简单归因于共享 BERT、数据管线或阈值 0.5 必然过高。
3. 故障范围集中于当前 T0 目标及其 minibatch 优化方式。

### 4.2 不能成立的结论

1. 不能宣称 B0 或 B1 总体优于所有 PU 方法。
2. 不能宣称 T0 的唯一失败原因已经得到因果证明。
3. 不能进行显著性或总体优胜结论：目前只有一个 fold 和一个 seed。
4. 不能用当前 development 结果事后选择更低阈值，再把结果当作预注册主结果。

---

## 5. 严格失败审计

审计只读取三个已完成 checkpoint 和 inner-development 数据；未训练模型、未调阈值、未输出原文、ID 或逐样本分数。

### 5.1 分数分布

| Arm | Heldout 分数均值 | q95 | q99 | 最大值 | 越过 0.5 的预测数 |
|---|---:|---:|---:|---:|---:|
| B0 | 0.00884 | 0.01135 | 0.12424 | 0.96830 | 171 |
| B1 | 0.00899 | 0.01503 | 0.10626 | 0.94246 | 179 |
| T0 | 0.04977 | 0.16385 | 0.28518 | 0.46822 | 0 |

T0 不是把所有概率压到接近零。它产生了较宽的中等分数区间，但没有形成 B0/B1 那样的高置信正例尾部。

### 5.2 遮蔽正例分数

| Arm | 平均值 | 中位数 | q75 | 最大值 | 恢复数 |
|---|---:|---:|---:|---:|---:|
| B0 | 0.2372 | 0.0870 | 0.3983 | 0.9366 | 17 / 78 |
| B1 | 0.2496 | 0.0737 | 0.5077 | 0.8922 | 21 / 78 |
| T0 | 0.1709 | 0.1092 | 0.3539 | 0.4451 | 0 / 78 |

### 5.3 有效优化覆盖

- 总训练 batch：6648。
- 零信号 batch：2669，占 **40.15%**。
- `uses` 是覆盖最高的类别，也只在 35.08% 的 batch 中有效。
- `targets` 为 21.22%，`indicates` 为 9.25%。
- `controls`、`delivers`、`exploits`、`has` 各只有 8 个有效 batch，占 **0.12%**。

### 5.4 nnPU 截断检查

18 个类别最终 full-view `raw_negative_risk` 全部为正，`negative_risk_clamped` 与原值相同。最终状态没有发生 nnPU 零截断，因此“负风险被截为零”不是当前失败的直接解释。

### 5.5 当前最有证据支持的机制解释

当前证据更支持以下组合机制：

1. 类别先验很小，使正风险项权重弱；
2. 类别只有在当前 batch 出现可见正例时才参与更新；
3. 40.15% batch 完全没有 T0 信号，稀有类更新覆盖极低；
4. 模型学到中等概率，但没有形成高置信正例尾部。

这是下一方法设计的证据基础，不是唯一原因的因果证明。

---

## 6. 为什么当前材料不足以形成论文贡献

1. 负结果只覆盖一个 fold、一个遮蔽率和一个 seed。
2. 失败审计解释了可疑机制，但没有证明一种修复能够稳定改善结果。
3. B0/B1 的差异很小，当前数据不能支持新的方法优越性论断。
4. 没有跨遮蔽率、跨 fold、跨 seed 的鲁棒性证据。
5. 当前工作可以作为方法开发依据和论文中的失败分析，但不能单独承担主要方法贡献。

---

## 7. 推荐的新方法：双流覆盖感知 classwise nnPU

### 7.1 方法名称建议

中文：**双流覆盖感知类别级非负 PU 风险**  
英文工作名：**Dual-Stream Coverage-Aware Classwise nnPU (DSC-nnPU)**

最终论文命名应在实现和验证后冻结；当前名称仅用于区分新方法与失败的 T0。

### 7.2 核心设计

每个优化步使用两个独立数据流：

1. **均匀行流 U**：从全部可见训练行均匀采样，用于估计每类的边际负风险 `R_c^u`。
2. **类别均衡正例流 P**：按关系类别轮换或均衡抽取可见正例，用于估计 `R_c^+` 和 `R_c^-`。

每类仍采用：

`π_c R_c^+ + max(0, R_c^u - π_c R_c^-)`

但正风险不再要求“均匀 minibatch 恰好包含该类正例”。训练调度明确保证每个有可见正例的类别在固定窗口内获得更新；均匀 U 流继续保持边际风险估计，避免只对正例过采样后扭曲未标注分布。

### 7.3 为什么优先推荐

1. 直接对应审计发现的零信号和稀有类低覆盖问题。
2. 保留 classwise PU 的核心问题设定，不把未解析关系重新硬标成负类。
3. U/P 双流把“均匀估计未标注边际”和“保证正类覆盖”分开，解释清晰。
4. 可以用明确消融检验每个部件，而不是一次加入多个不可解释技巧。

### 7.4 必须记录的新诊断量

- 每类每 epoch 的正例流样本数和有效更新步数；
- 每类 U/P 风险分量与是否发生非负截断；
- 零信号 optimizer step 数，目标应为 0；
- 每类 heldout 分数上尾与遮蔽正例分数；
- 采样重复率，防止极稀有类被无限重复导致过拟合。

### 7.5 必要消融

| 变体 | 目的 |
|---|---|
| T0 原实现 | 已有失败参照，只引用现有结果，不重复训练 |
| T1-S：仅 positive-aware sampling | 判断提高正例覆盖本身能否恢复高置信尾部 |
| T1-D：双流但不做类别均衡 | 判断 U/P 风险解耦的独立作用 |
| DSC-nnPU：双流 + 类别覆盖调度 | 完整新方法 |

如计算预算有限，优先保留 T0、T1-S 和完整 DSC-nnPU。

---

## 8. 备选方法及优先级

### 8.1 第二优先：跨 batch 风险记忆或 epoch 级聚合

为每类维护跨 batch 的正风险统计，使无正例 batch 仍能利用近期估计。优点是减少类别跳过；风险是缓存值与当前参数不同步，梯度定义和理论解释更复杂。建议仅在双流方法不能稳定训练时考虑。

### 8.2 第三优先：类别先验或正风险重加权

对极小 prior 使用温度、下限或额外权重。该方案实现简单，但会改变 PU 风险含义并引入敏感超参数。若采用，权重必须只根据 inner-train 可见统计预先冻结，并单独做消融。

### 8.3 当前不建议作为主修复

- 事后把阈值从 0.5 调低；
- 直接增加 epoch；
- 只扩大模型或更换编码器；
- 根据本次 heldout 逐类选阈值；
- 把模型生成标签当作人工金标补入训练。

这些操作没有直接解决已观测到的有效更新覆盖问题，且容易引入开发集过拟合或改变研究问题。

---

## 9. 建议冻结的下一阶段实验计划

### 阶段 A：纯合成与执行器验证

1. 为双流 sampler 和损失写独立实现。
2. 用纯合成张量验证：U/P 风险公式、稀有类轮换、无可见正例类的 abstain 行为、固定 seed 可复现。
3. 使用 tiny CPU BERT 路径完成至少一次前向、反向和 optimizer step。
4. 建立新的设计 freeze、执行 freeze 和 fail-closed 单单元授权。

通过标准：零信号 step 为 0；每个可见类别在冻结窗口内均被更新；无 outer/锁定访问。

### 阶段 B：单单元机制门槛

只运行 `fold02 / mask=0.1 / seed=13`，比较现有 B0、B1、T0 与新方法，不覆盖任何旧产物。

建议继续门槛：

- 新方法产生非零正预测；
- 遮蔽正例恢复数大于 0；
- development micro-F1 不出现 T0 式退化；
- 稀有类有效更新覆盖达到冻结下限；
- 无非有限 loss、无输出坍缩。

未通过即停止并审计，不扩矩阵。

### 阶段 C：小矩阵

门槛通过后，建议先运行：

- folds：02、03；
- mask rates：0.1、0.3、0.5；
- seed：13；
- arms：B0、B1、DSC-nnPU。

共 18 个单元。mask=0 可作为完整标注 sanity check，但是否进入主矩阵请导师确认。

### 阶段 D：多 seed 确认

在阶段 C 冻结后，为主要对比增加至少 3 个 seed。统计单位必须是配对的 `fold × mask rate × seed`，报告均值、标准差或置信区间、配对效应量与多重比较处理。只有到这一阶段，才讨论稳健优势或论文主张。

---

## 10. 拟用于论文的安全表述边界

### 当前可以写

> 在一个预先冻结的 inner-development 单元中，原始 classwise nnPU 目标在固定阈值下没有产生正预测。聚合审计显示，该目标存在大量零信号 batch 和极低的稀有类更新覆盖，同时最终风险未触发非负截断。这一结果促使我们设计显式分离均匀未标注风险估计与类别覆盖正风险估计的双流方法。

### 当前不能写

- “我们的方法显著优于 B0/B1”；
- “已经证明 minibatch 稀疏覆盖是唯一失败原因”；
- “T0 在所有设置中失败”；
- “降低阈值即可解决问题”；
- “模型辅助材料构成人工金标”。

---

## 11. 复现与证据索引

### 11.1 设计与审计协议

- `cti_improvement/research_protocol/cti_observed_state_training_design_freeze.json`  
  SHA256: `0d3d27ea30e019a133597b57ba5229cbb3f372eb99a1473418f1d4b16e41601e`
- `cti_improvement/research_protocol/cti_observed_state_training_execution_freeze.json`  
  SHA256: `0a91b02a3947a78bf7d487d9a6f5d677175656df46aacfde0a194416a4c5cc81`
- `cti_improvement/research_protocol/cti_observed_state_failure_audit_execution_freeze.json`  
  SHA256: `a336256f30fece809a45adea94dc3a1aae1006d4842a706f02eee89cce1fcf73`

### 11.2 已完成训练单元 manifest

- T0: `717f65d2d784580c754b2071f4aeb0b2d8155512857fef152ec52dae7cacfa04`
- B0: `bc2ac938c78acc436939c1c04a57fdf06299d91fcbbc09f7277053bf7d1ed5da`
- B1: `e3e6b7e6359ebde9377d1efe83f2780e22649db687e4b5a1b55466c648f2e84f`

### 11.3 聚合失败审计

- completion manifest: `e33dbbcaff3d11601d8ed39d1a5aca9a40bd0496520a99111b9aa0b26b55aa76`
- aggregate audit: `96ee94befc2a5d0c54252650d3986f6832cd7d4a9e81195f326dc704f0a1325b`
- authorization snapshot: `ee088f9bb78ad96314a15788d76032f5a80b806a7b2ad37743f88a9ed1c989b9`

### 11.4 环境

- 服务器 Conda：`/root/autodl-tmp/lgx/conda_envs/unirel38`
- Python：该环境的 `bin/python`
- PyTorch：`2.1.2+cu121`
- Transformers：`4.46.3`
- GPU：NVIDIA GeForce RTX 4090

---

## 12. 导师意见记录区

**是否继续该研究问题：**  

**是否认可匹配 B0/B1 对照：**  

**是否同意 DSC-nnPU 主方案：**  

**是否需要历史完整系统对照：**  

**允许的实验预算：**  

**论文最低贡献标准：**  

**其他修改意见：**  
